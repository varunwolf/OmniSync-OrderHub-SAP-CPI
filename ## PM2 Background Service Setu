
The Inventory API is managed using PM2 so that the Node.js server keeps running in the background even after closing the SSH terminal.

PM2 process name:

omnisync-inventory-api

Start command:

```bash
cd ~/inventory-api
pm2 start server.js --name omnisync-inventory-api
